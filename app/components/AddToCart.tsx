'use client';
import React from 'react';

const AddToCart = () => {
  return (
    <div>
      <div className="btn btn-primary" onClick={() => console.log('Clicked')}>
        Add to Cart
      </div>
    </div>
  );
};

export default AddToCart;
